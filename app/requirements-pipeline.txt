# Pipeline agent dependencies
# Install with: pip install -r requirements-pipeline.txt
#
# Note: Pillow is NOT listed here — it fails to build on Python 3.14.
# It's only needed for illustration edge-processing (Agent 4) which isn't
# wired yet. Will be added back once we sort out the Pillow/Python 3.14 issue.

# ── Agent 1: Ingestion ────────────────────────────────────────────────────────
python-docx>=1.1.2      # .docx / .doc support (original)
pdfplumber>=0.10.0      # .pdf text extraction
ebooklib>=0.18          # .epub parsing
beautifulsoup4>=4.12.0  # HTML cleaning for EPUB content

# ── All agents ────────────────────────────────────────────────────────────────
google-genai>=1.0.0
reportlab>=4.0
tqdm>=4.66.0
colorama>=0.4.6
